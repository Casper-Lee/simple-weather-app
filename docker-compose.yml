version: '3.8'
services:
  app:
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
    healthcheck:
      test: ['CMD-SHELL', 'curl --insecure -sS http://localhost:3000 || exit 1']
      interval: 10s
      timeout: 5s
      retries: 3
